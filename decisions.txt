Although FlowScript is based heavily on lox, it is not lox, and does not necessarily adhere to its specification.
This document will aim to point out all the design decisions FlowScript took that stray away from lox
Decisions marked as (REVISE) take priority in terms of being revisited in the future


Logical Operators:
    - In FlowScript, a logical expression returns either true or false. That is different from lox, in which a logical expression returns 
    the left or right operands, as it only guarantees that logical expressions return a value of the correct "truthiness"

    - In FlowScript, logical operators don't do short ciruiting, so for a statement like "if (false and true)", FlowScript checks both operands
    and doesn't short circuit after seeing the first operand is false (REVISE)

Functions:
    - In FlowScript, functions do not have a maximum amount of arguments, unlike lox, which has a maximum of 255.

Additions:
    - FlowScript has syntax for comma seperated expressions outside of function arguments, Which lox doesn't support.